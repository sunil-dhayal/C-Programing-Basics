#include<stdio.h>
void main()
{ char s[10]="SAHIL",t[10]="SAGAR";
int i;
for(i=0;i<=4;i++)
{if(s[i]==t[i])continue;
 else if(1)
    {printf("not the similar string at character %d",i+1);
    break;}
        }}
